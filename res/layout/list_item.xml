<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >
    
    
    <ImageView
        android:id="@+id/icon"
        android:layout_width="30dip"
        android:layout_height="30dip"
        android:contentDescription="@string/headerDescription"
        android:src="@drawable/ic_action_person"
        />

    <TextView
        android:id="@+id/textViewInListViewName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/icon"
        android:layout_toEndOf="@id/icon"
        android:layout_marginBottom="2dp"
        android:paddingBottom="2dp"
        android:typeface="sans"
        android:textStyle="bold"
        android:textSize="23sp" />

    <TextView
        android:id="@+id/textViewInListViewPhone"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textViewInListViewName"
        android:textSize="18sp"
        android:typeface="sans"
        android:layout_alignParentBottom="true"
       	android:layout_marginBottom="5dp"
       	android:paddingBottom="2dp"
        android:layout_toRightOf="@id/icon"
        android:layout_toEndOf="@id/icon"
         />
    
  
    <!-- android:layout_alignTop="@+id/textViewInListViewName"/> -->

</RelativeLayout>